/**
 * Rutas de navegación de la app (Expo Router).
 * Usar estas constantes para navegación en lugar de strings hardcodeados.
 */
export const ROUTES = {
  LOGIN: '/(auth)/login',
  REGISTER: '/(auth)/register',
  HOME: '/(app)',
} as const;
